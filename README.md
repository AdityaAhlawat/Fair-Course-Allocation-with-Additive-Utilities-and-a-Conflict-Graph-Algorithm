To get the Graph Average Runtime Comparison (40 stuents), run IncreasingCFixedSGraph.py and the graph will generate after a few minutes. You can view progress of the graph generation from the terminal.

To get the Graph Average Runtime Comparison (200 courses), run IncreasingSFixedCGraph.py and the graph will generate after a few minutes. You can view progress of the graph generation from the terminal.

To get the Graph Average EF Violations among Students (40 Students), run EFViolationsWIncreasingCourses.py and the graph will generate after a few minutes. You can view progress of the graph generation from the terminal.

To get the Graph Average EF Violations towards Charity (40 students), run EFViolationsOnCharity.py and the graph will appear after a few minutes. You can view progress of the graph generation from the terminal.

To generate the smaller real world public dataset results, run SmallPublicDatasetResults.py

To generate the larger real world public dataset results, run LargePubliDatasetResults.py